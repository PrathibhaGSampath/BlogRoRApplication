<%= form_for [@post, @comment] do |comment| %>

  <%= comment.label :comment %>:
  <%= comment.text_field :description %><br />

  <%= submit_tag 'Save'%>
  <%= link_to 'Back', posts_path %>
<% end %>